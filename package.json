{
  "name": "csc-307-app",
  "version": "1.0.0",
  "description": "App for Cal Poly's CSC-307",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd packages/react-frontend && npm start",
    "dev": "cd packages/express-backend && npx nodemon backend.js"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "create-react-app": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
